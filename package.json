{
  "name": "stillwater",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "deploy": "npm run build && node scripts/deploy.mjs"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
